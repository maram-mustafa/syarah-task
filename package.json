{
  "name": "syarah",
  "private": true,
  "version": "1.0.0",
  "packageManager": "pnpm@10.12.3",
  "scripts": {
    "clean": "pnpm turbo run clean",
    "clean-all": "pnpm turbo run clean-all",
    "build-all": "pnpm turbo run build --force --no-cache"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@changesets/cli": "^2.29.5",
    "typescript": "^5.8.3"
  }
}